# Klein
🖇️ The most intelligent URL shortening framework on the planet.

## Roadmap
- Encode endpoint
POST `/api/hyperlink`
- Decode endpoint
GET `/api/hyperlink`
- Redirect endpoint
GET `/:slug`
- GUI endpoint
GET `/app/*`
